#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n, a, b, c, d; string s;
    cin >> n >> a >> b >> c >> d >> s;
    

}

int main(){
    ios::sync_with_stdio(false); cin.tie(0);
    int T = 1;
    // cin >> T;
    for(int i = 0; i < T; i++) {
        solve();
    }
}