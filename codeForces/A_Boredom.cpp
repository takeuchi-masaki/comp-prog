#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    vector<int> a(n), dp(n+1);
    for(int& i:a)cin>>i;
    for(int i = 0; i < n; i++){
        // dp[i]=max();
    }

}

int main(){
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int T = 1;
    // cin >> T;
    for(int i = 0; i < T; i++) {
        solve();
    }
}