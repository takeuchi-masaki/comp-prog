#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    vector<int> h(t);
    for(int i = 0; i < t; i++){
        int add;
        cin >> add;
        h[i] = add;
    }
    int nec = 0;
    /*
        recursive solution
        initial l, r = begin, end
        when curr hits min value
            split before/after

    */
}

