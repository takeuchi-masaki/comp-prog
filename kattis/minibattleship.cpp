#include <bits/stdc++.h>
using namespace std;

int main(){
    // freopen("input.txt","r",stdin); freopen("out.txt","w",stdout);
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector< vector<char> > orig_grid(n, vector<char>(n));
    for(int i = 0; i < n; i++){
        for(int j = 0; )
    }
    for(int i = 0; i < k; i++){

    }
}