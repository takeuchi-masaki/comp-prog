#include <bits/stdc++.h>
using namespace std;

int main(){
    // freopen("input.txt","r",stdin); freopen("out.txt","w",stdout);
    ios::sync_with_stdio(false); cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    for(int i = 0; i < k; i++){

    }
    for(int i = 0; i < n-1; i++){

    }

    // happiness = sum skill of players
    // node count, node parent
    // sort nodes by number of matches played
    
}